# 本地研究缓存（不发布）

此目录仅供追溯参考来源，不由游戏加载、不打进 ZIP，也不应整体公开。

冻结检查发现原 `photos.json` 与 `unsplash.json` 实际为请求被拦截后返回的 HTML 验证页，而非图片 API 数据。已按实际格式改名为 `photos.response.html`、`unsplash.response.html`，内容字节未改；不将其当作可用素材、许可证或有效 JSON。其余原始资料保留。

这里存在第三方页面/图片与原始语音，保留不等于取得分发权。具体授权边界见项目根目录 `NOTICE.md`。
